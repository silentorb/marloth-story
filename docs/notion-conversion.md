# Notion export → `content/` markdown

This document has moved to the feature spec:

**[features/notion-import.md](./features/notion-import.md)**

That file is the authoritative design spec for the Notion import pipeline (requirements, rationale, behavior, and operations).
