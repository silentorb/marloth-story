Git-tracked SQLite property graph. Canonical file: `marloth.sqlite`.

Regenerate with `bun run notion:import -- --clean` from the repository root.

See [`docs/features/marloth-db.md`](../docs/features/marloth-db.md).
