---
description: Rippledoc feature — read authoritative spec before changing watcher or pipeline
globs: packages/rippledoc/**
alwaysApply: false
---

# Rippledoc

You are working on the content watcher / pipeline.

Read the authoritative feature spec before making changes:

[`docs/features/rippledoc.md`](../../docs/features/rippledoc.md)

Configuration precedence: **CLI flags > environment > defaults** (`RIPPLEDOC_*` preferred; `MARLOTH_*` legacy).
