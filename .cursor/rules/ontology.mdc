---
description: Design ontology — read for domain meaning before editing graph data
globs: content/**,exports/**
alwaysApply: false
---

# Design ontology

You are working with Marloth **design nodes** (scenes, features, inspirations, products, etc.).

Read the storage-agnostic domain model before interpreting or changing data:

[`docs/ontology.md`](../../docs/ontology.md)

Pair with schema docs when querying or editing the graph:

- [`docs/features/marloth-db.md`](../../docs/features/marloth-db.md) — edit `content/` (see root [`AGENTS.md`](../../AGENTS.md) **Graph data workflow**)
- [`docs/features/notion-import.md`](../../docs/features/notion-import.md) — legacy export mining only

**Terminology:** “project feature” = `./docs/features/` tooling; “feature” = graph design node; **node** / **relationship** / **page** per root [`AGENTS.md`](../../AGENTS.md).
