---
description: Marloth content-first graph — edit content/, not SQLite cache
globs:
  - packages/marloth-db/**
  - content/**
  - docs/features/marloth-db.md
alwaysApply: false
---

# Marloth database

You are working on the git-tracked design corpus (`content/`) or `packages/marloth-db/`.

Read before making changes:

- [`docs/features/marloth-db.md`](../../docs/features/marloth-db.md)
- Root [`AGENTS.md`](../../AGENTS.md) — **Graph data workflow**

**Default:** edit `content/` (`{id}.md`, `relationships.json`, `dynamic-fields.json`) or use `ContentStore` / the editor API. **Do not** edit `data/marloth.sqlite` directly or run `bun run notion:import` / `--clean` for routine work.

**Archival exports:** if data exists only in `./exports/`, mine the relevant files and upsert into `content/` — see [`docs/features/notion-import.md`](../../docs/features/notion-import.md) (legacy).

For design node semantics: [`docs/ontology.md`](../../docs/ontology.md). Terminology: **node**, **relationship**, **page** (see root [`AGENTS.md`](../../AGENTS.md)).
